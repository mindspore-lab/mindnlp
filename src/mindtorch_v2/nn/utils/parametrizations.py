"""Parametrizations module (minimal compatibility stub).

Provides the ``torch.nn.utils.parametrizations`` namespace so that
``import torch.nn.utils.parametrizations`` does not raise.  Individual
parametrizations (e.g. ``weight_norm``, ``spectral_norm``,
``orthogonal``) will be added here as needed.
"""
